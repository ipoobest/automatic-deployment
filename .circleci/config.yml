version: 2
jobs:
  build:
    docker:
      - image: python:3.7
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
      - run: git push root@178.128.105.16:ndd-production master
        